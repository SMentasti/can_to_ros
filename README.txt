Based on example from this: https://github.com/questrail/pycan

to run use the command "python -m can.viewer -b 500000 -i pcan" in root folder (above can folder)
se non funziona carica i driver:sudo modprobe pcan
